[package]
name = "portalis-nexus-demo"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# One binary per PLAN.md step, kept forever: an executable changelog and the
# acceptance suite for everything except the UI. The unnumbered milestone
# demos this crate used to hold were replaced by 01 to 08 and deleted.
[dependencies]
anyhow = "1"
axum.workspace = true
backend = { path = ".." }
blake3.workspace = true
redb.workspace = true
ed25519-dalek.workspace = true
portalis-nexus-client.workspace = true
portalis-nexus-protocol.workspace = true
portalis-nexus-storage.workspace = true
portalis-nexus-server.workspace = true
tokio.workspace = true
tracing-subscriber.workspace = true
x25519-dalek.workspace = true

[lints]
workspace = true
